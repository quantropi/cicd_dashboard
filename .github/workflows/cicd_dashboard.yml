name: CICD Dashboard Workflow
on:
  workflow_dispatch:

  repository_dispatch:
    types: [cicd_dashboard]

jobs:
  update_data:
    runs-on: ott-ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Trigger template for cicd_dashboard
        run: |
          echo "abc"